1. Python / Programming

What is the difference between a generator and a normal function? Why are generators memory efficient?

How does Python’s GIL (Global Interpreter Lock) affect multithreading?

Write Python code to flatten a nested list without using libraries.

What are context managers in Python? Write a custom context manager.

How does exception handling work? Write a function that retries 3 times before failing.

What is the difference between is and == in Python?

Explain lambda functions. Using lambda, sort a list of tuples by second element.

Write a program to find the k most frequent elements in a list.

What is memoization? Apply memoization to a recursive Fibonacci function.

Explain time complexity of common Python data structures (list, dict, set).

2. SQL / Database

Write a query to find the top 3 highest-selling products per month.

How do you delete duplicate rows from a table while keeping only one copy?

Write SQL to find customers who placed orders in 2024 but not in 2025.

Explain window functions with an example.

How would you find the earliest and latest transaction per customer?

Write SQL to pivot monthly sales into columns (Jan, Feb, … Dec).

What is the difference between ROW_NUMBER(), RANK(), and DENSE_RANK()?

How do you identify orphan records in SQL?

Write SQL to find products with sales above average product sales.

What are CTEs? Show how to use a recursive CTE.

3. Machine Learning / Statistics

Explain the Central Limit Theorem in simple terms.

What is heteroscedasticity? How do you detect and fix it?

When would Logistic Regression fail?

Explain different types of distance metrics used in clustering.

Difference between KNN and K-means.

What is feature leakage? Give an example.

Why do we scale data? Which models require scaling?

How do you evaluate a regression model besides RMSE and MAE?

Explain Gradient Descent. What happens if learning rate is too small or too large?

What is SHAP? How is it useful?

4. Pandas / Data Wrangling

Write pandas code to remove all rows where more than 50% values are missing.

How to detect columns with constant values and drop them?

What is the difference between merge, join, and concat?

How to calculate month-over-month growth using pandas?

Write code to apply different functions to different columns using agg().

How to convert a long dataframe into wide format using pivot?

Explain vectorization in pandas.

How would you optimize a slow pandas pipeline?

How to detect highly correlated features quickly?

Write code to group customers into bins based on total purchase amount.

5. Case Study / Business Scenarios

A retail client sees high return rates for a specific category. How would you investigate?

You’re asked to build a demand forecasting model but data is missing for some months. How would you handle it?

A bank wants to reduce loan default rate. What approach would you take?

An e-commerce company wants to cluster customers. What features would you engineer?

You have 200K customer complaints text messages. How will you categorize them?

A company wants to measure impact of a 10% discount campaign. How would you evaluate it?

How would you design a fraud detection solution?

A client wants to automate their sales dashboard. What pipeline would you design?

A product’s sales dropped suddenly. What steps would you take to analyze the cause?

A telecom company wants to reduce call drops. How can data science help?

6. Behavioral / Delivery-Focused (Accenture Style)

Accenture projects require working with cross-functional teams. Tell me about such an experience.

Describe a time you faced a data quality issue and how you solved it.

How do you communicate technical findings to a client who is non-technical?

What do you do when a model performs well offline but poorly in production?

Tell me about a time you disagreed with a stakeholder’s approach.
